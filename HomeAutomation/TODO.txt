Features:
- Keywords to start listening. Different keywords will have different weights. EX: 'walter' has weight of 1, while 'activate' has 0.8
- Find places where a factory can be used, eliminate unnecessary creations in ApplicationFactory
- News reading
- Room sensors
- Electronic calculators (resistance, capacitance, etc.)
- Spelling help. use speech recognition
- Repeat last phrase if requested
- Slideshow
- Webpage Alerts Ex: Alert for Linux on r/games
- have different listening states:
	Not listening: no audio will trigger any events
	Listening for relevent: will trigger commands if input is very obviously a command. will switch back to "listening" if lots of irelevent text is heard.
	Listening: will trigger commands only if {name} is said before the phrase
- When phone is taken off cradle, pause music / lower volume
- Use 'pactl set-sink-volume 3 35%' to set volume
- Use dynamic keywords to check for commands vs regular speech
- math using math keywords to wolfram alpha
- add capabilities in taglist to have word types. ex: "\NUMBER times \NUMBER" ex2: "turn on \DB_OBJECT". Most word types will be based on other tags, bu some such as \number will have special definitions
	possibility: use a mix of teglist items and regex. ex: "set \-DB_OBJECT -d" would turn into "set light|stereo|heater -d" then checked with regex
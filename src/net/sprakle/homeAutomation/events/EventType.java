/*
 * Defines possible event that can occur within this application
 */

package net.sprakle.homeAutomation.events;

public enum EventType {
	USER_TEXT_RECIEVED,
	DB_FILE_UPDATED,
	TAGLIST_FILE_UPDATED,
	RELOAD,
}
